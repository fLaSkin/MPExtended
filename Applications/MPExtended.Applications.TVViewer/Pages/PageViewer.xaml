<Page x:Class="MPExtended.Applications.TVViewer.Pages.PageViewer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="471"
	Title="PageViewer">
    <Grid Width="469">
        <ListBox x:Name="lbChannels" Margin="0,0,319,0">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Label Content="{Binding DisplayName}" Width="120"/>
            </DataTemplate>
        </ListBox.ItemTemplate>
        <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
                <EventSetter Event="ListBoxItem.Selected" Handler="lbChannels_Selected" HandledEventsToo="True"/>
            </Style>
        </ListBox.ItemContainerStyle>
    </ListBox>
        <Grid x:Name="MediaArea" Margin="156,0,0,0"></Grid>

        </Grid>
</Page>
