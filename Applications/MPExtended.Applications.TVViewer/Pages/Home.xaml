<Page x:Class="MPExtended.Applications.TVViewer.Pages.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
          mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="601" Width="600"
	Title="Home">
    <Page.Resources>


    </Page.Resources>
    <Grid>
        <Button x:Name="btnRefresh" Click="btnRefresh_Click"></Button>
            <DatePicker x:Name="dpStartDate" Margin="277,0,199,274"></DatePicker>
        <DatePicker x:Name="dpEndDate" Margin="415,0,61,274"></DatePicker>
        <ListBox x:Name="lbChannels" Margin="0,32,0,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="{Binding Channel.DisplayName}" Width="120"/>
                        <ListBox   ItemsSource="{Binding Programs}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>                              
                                        <Label  Content="{Binding Title}" Width="80">
                                            <!-- <Label.Width>
                                               <MultiBinding Converter="{StaticResource x:ResourceKey=MyMultiValueConverter}">
                                                    <Binding Path="StartTime" />
                                                    <Binding Path="EndTime" />
                                               
                                        </MultiBinding>
                                            </Label.Width>-->
                                        </Label>
                                  
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</Page>
